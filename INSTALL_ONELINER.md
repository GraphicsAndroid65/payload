# GamerX AI - One-Liner Installation

## 🚀 Super Simple Installation

Users can install GamerX AI with **ONE COMMAND**:

```powershell
iex (iwr 'https://raw.githubusercontent.com/GraphicsAndroid65/payload/main/install.ps1').Content
```

That's it! No downloads, no admin, no hassle!

---

## 📋 Installation Steps

### Step 1: Open PowerShell
Open PowerShell (no admin needed!)

### Step 2: Copy the One-Liner
```powershell
iex (iwr 'https://raw.githubusercontent.com/GraphicsAndroid65/payload/main/install.ps1').Content
```

### Step 3: Paste and Press Enter
The script will automatically:
- Download GamerX AI
- Set up configuration
- Update PowerShell profile
- Create aliases

### Step 4: Restart PowerShell
Close and reopen PowerShell

### Step 5: Start Using!
```powershell
ai "write a python function"
```

---

## 💻 Usage After Installation

### Single Query
```powershell
ai "write a python function to reverse a string"
```

### Interactive Chat
```powershell
ai -chat
```

### View History
```powershell
ai -history
```

### Clear History
```powershell
ai -clear
```

### Get Help
```powershell
ai -help
```

### Short Alias
```powershell
gx "write javascript code"
```

---

## 🔗 How It Works

The one-liner:
1. `iwr` = Downloads the installation script from GitHub
2. `iex` = Executes the script immediately
3. Script automatically sets up everything

**No files to download manually!**

---

## 🎯 Share This Command

Tell others:

```
Open PowerShell and paste:
iex (iwr 'https://raw.githubusercontent.com/GraphicsAndroid65/payload/main/install.ps1').Content
```

---

## ✨ Features

✅ **No Admin Required**
- Works on any Windows device
- Works on corporate networks
- Works on restricted devices

✅ **One Command**
- Super simple
- Easy to remember
- Easy to share

✅ **Automatic Setup**
- Downloads latest version
- Configures everything
- Ready to use immediately

✅ **Code-Only Output**
- No explanations
- No comments
- Pure code

✅ **Full Functionality**
- Interactive chat mode
- Chat history saved
- Gemini 2.0 Flash API

---

## 🐛 Troubleshooting

### "Execution Policy" Error

If you get an error about execution policy, run:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser -Force
```

Then run the one-liner again.

### "Command Not Found" After Installation

Close and reopen PowerShell completely.

### "Download Failed" Error

Make sure you have internet connection. Try again or use manual installation:

```powershell
git clone https://github.com/GraphicsAndroid65/payload.git
cd payload
.\install.ps1
```

---

## 📊 Installation Locations

After installation:

- **Installed**: `C:\Users\YourName\GamerX-AI\`
- **Config**: `C:\Users\YourName\AppData\Roaming\ai-chatbot\`
- **History**: `C:\Users\YourName\AppData\Roaming\ai-chatbot\chat_history.json`

---

## 🎓 Quick Reference

| Command | Purpose |
|---------|---------|
| `ai "query"` | Single code query |
| `ai -chat` | Interactive chat mode |
| `ai -history` | Show chat history |
| `ai -clear` | Clear history |
| `ai -help` | Show help |
| `gx "query"` | Short alias |

---

## ✅ Installation Complete!

Your GamerX AI is ready to use!

```powershell
ai "write a hello world program"
```

Enjoy! 🎉

---

**Version**: 1.0
**Installation Method**: One-Liner (iex)
**Admin Required**: ❌ NO
**Time to Install**: < 1 minute
**Brand**: GamerX AI
**Status**: ✅ Production Ready
